# Active when spring.profiles.active=dev (default for local runs in application.properties)
# Seeds a demo pharmacy + PHARMACY_ADMIN and CASHIER users when the users table is empty

app.data.seed=true
spring.jpa.show-sql=true
